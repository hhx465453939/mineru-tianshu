# Frontend Environment Variables
# 前端环境变量配置

# API Base URL（可选）
# 如果不设置，生产环境会自动使用当前域名 + VITE_API_PORT
# VITE_API_BASE_URL=http://your-domain.com:18657

# API 端口（默认 18657）
# 生产环境中，前端会自动使用当前域名 + 此端口访问后端
VITE_API_PORT=18657
